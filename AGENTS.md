# Agent Instructions

- Create a feature branch before making any change.
- Before starting a new feature, switch to `master`, update it from `origin/master`, then create the feature branch from updated `master`.
- Show the plan and get explicit permission before changing files.
- Refer to `/Users/buddhimawijeweera/archived/inventory-app-backend/openapi.yml` as the source of truth for API routes, request/response shapes, and auth details.
- Keep changes aligned with the app's current framework, styling conventions, component patterns, and design system.
- Ask for explicit permission before adding any new dependency, and add dependencies only when they are essential.
